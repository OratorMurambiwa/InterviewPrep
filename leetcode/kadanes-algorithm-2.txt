        """
        cmax = 0
        cmin = 0
        gmax = nums[0]
        gmin = nums[0]
        total = 0

        for num in nums:
            cmax = max(cmax + num, num)
            cmin = min(cmin+num, num)
            total += num 
            gmax = max(gmax, cmax)
            gmin = min(gmin, cmin)
        
        if gmax > 0:
            return max(gmax, total-gmin)
        else:
            return gmax
        